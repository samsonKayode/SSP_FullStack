input {
    tcp {
      port => 12210
      codec => json
    }
}
filter {
  date {
    match => [ "timeMillis", "UNIX_MS" ]
  }
}
output {
  elasticsearch {
    index => demo_index_name
    hosts => ["http://elasticsearch:9200"]
  }
}
